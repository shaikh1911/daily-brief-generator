### 🔑 API Configuration
To protect security, the API key has been removed from the source code. 
To run this project:
1. Obtain an API key from [Groq Cloud](https://console.groq.com/).
2. Open `DailyBriefService.java`.
3. Enter your key in the `apiKey` variable.
4. Run the Spring Boot application.
